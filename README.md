### Book service Test FastAPI application

- To start the app locally simply use the `just` command
   ```bash
    just dev
   ```
  
- Use the Bruno collection to test the various APIs by importing the file `PythonFoundry-API-collection.json` into your Bruno application
- To use the linter simply run the following command
  ```bash
    just lint
  ```